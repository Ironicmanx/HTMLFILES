const input = document.querySelector('input')
const input = document.querySelector('output')
const button = document.querySelector('button')


button.addEventListener('click',() => {
    
let sek = input.addEventListener('button')


})
